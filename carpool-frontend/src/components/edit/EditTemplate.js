import React from 'react';
// import Edit from "./Edit";

const EditTemplate = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default EditTemplate;